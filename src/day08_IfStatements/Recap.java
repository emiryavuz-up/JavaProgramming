package day08_IfStatements;

public class Recap {

    public static void main(String[] args) {

    /*
Java Day08:
Topics: Logical Operators (&&, ||, !)
		If Statements:
				single if


        // variables = container, to save data so you can reuse it
        // declare = DataType VariableName = Data;
        // DataTypes = 8 primivite int double char boolean , 1 non primitive = String
        // int integer - double for decimals - char for single charaters - boolean for true false
        // for char every single character has a number so even int can take 'A' but
        // you will get the number since int only can contain numbers
        // string of text // multiple characters // sequence of characters "something"
        // only "_" "$" can start variable name // dont use java reserved word for a variable name
        // concatenation = gives value to a string. concating a value (any type) to String, +

     */

    }


}
